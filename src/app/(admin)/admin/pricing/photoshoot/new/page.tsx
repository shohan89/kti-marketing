import PhotoshootForm from '../PhotoshootForm'

export const metadata = { title: 'New Photoshoot Package — KTI Admin' }

export default function NewPhotoshootPage() {
  return <PhotoshootForm initialData={null} />
}
